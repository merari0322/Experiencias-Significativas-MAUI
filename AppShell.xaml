<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Experiencias_Significativas_App.MAUI.Views"
       x:Class="Experiencias_Significativas_App.MAUI.AppShell">

    <!-- Define tus rutas -->
    <ShellContent Route="HomePage" ContentTemplate="{DataTemplate views:HomePage}" />
    <ShellContent Route="FollowUpPage" ContentTemplate="{DataTemplate views:FollowUpPage}" />
    <ShellContent Route="ExperiencePage" ContentTemplate="{DataTemplate views:ExperiencePage}" />
    <ShellContent Route="ProfilePage" ContentTemplate="{DataTemplate views:ProfilePage}" />

</Shell>
